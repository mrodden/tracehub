from setuptools import setup, find_packages


setup(
    name = "tracehub",
    version = "0.1.0",
    author = "mathewrodden@gmail.com",
    packages = find_packages(),
    install_requires=[
        "flask",
    ],
)
